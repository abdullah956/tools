"""App configuration for the onboarding questions app."""

from django.apps import AppConfig


class OnboardingQuestionsConfig(AppConfig):
    """Configuration for the onboarding questions app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "onboarding_questions"
