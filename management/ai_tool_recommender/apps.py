"""App configuration for AI Tool Recommender."""

from django.apps import AppConfig


class AiToolRecommenderConfig(AppConfig):
    """Configuration for AI Tool Recommender app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "ai_tool_recommender"
    verbose_name = "AI Tool Recommender"
