"""Apps for the submit resource form app."""

from django.apps import AppConfig


class SubmitresourceFormConfig(AppConfig):
    """Configuration for the submit resource form app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "forms.submitresource_form"
    verbose_name = "Submit Resource Form"
